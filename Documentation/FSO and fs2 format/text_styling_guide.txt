# Text Styling Guide

Special tags can be used to manipulate text strings in command and mission briefings, debriefings and in the fiction viewer.
**Important**: these tags only work in the contexts mentioned above. Do not use them in other places (such as in in-mission messages)!

## Colors

The color tag will work on all font until a space is encountered (that is, on a single word only). There needs to be a space after the tag, before the text to be colorized. There is a method to color spans of text (multiple words), which is discussed in the Misc section below.

**Example**: '$b This is $b blue $b text' will show everything except the "is" as blue.

### Color tags:
Bright White: $W
Standard White (default): $w
Black: $k
Standard Blue: $b
Bright Blue: $B
Standard Green: $g
Bright Green: $G
Standard Yellow: $y
Bright Yellow: $Y
Gray: $e
Silver: $E
Violet Gray: $v
Violet: $V
Standard Red: $r
Bright Red: $R
Pink: $p
Bright Pink: $P
Orange: $o

## Notes and examples

$quote will create double quotes.
    Goober is $quotesilly$quote = Goober is "silly".
$semicolon will create a semicolon.
$callsign places the pilot name in its place.
$rank places the player's rank in its place.
$f and $h will color the text with the default colors for Friendly (green) and Hostile (red).
Most special characters such as the comma ',', question mark '?', and exclamation point '!' cannot be colored.
To color spans of text use $c{ and $}.
    The $y{ quick $g{ brown fox $r jumps over $} the lazy $} dog. = The (default color) quick (yellow) brown fox (green) jumps (red) over (green) the lazy (yellow) dog. (default color)
    This method will color special characters such as the comma ',', question mark '?', and exclamation point '!'
    Color spans work across linebreaks as well (either manually-added, or automatic from wordwrapping).